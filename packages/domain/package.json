{
  "name": "@jpkrlove/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "prettier --check .",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "test": "vitest run",
    "build": "tsc --noEmit --project tsconfig.json"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "vitest": "4.1.10"
  }
}
