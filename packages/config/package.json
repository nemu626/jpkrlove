{
  "name": "@jpkrlove/config",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "prettier --check .",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "build": "tsc --noEmit --project tsconfig.json"
  }
}
