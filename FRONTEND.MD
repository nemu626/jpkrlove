# jpkrlove 프런트엔드

## 1. 결정 요약

첫 MVP는 Expo와 TypeScript로 개발하는 iOS/Android 단일 React Native 앱이다. 회원용 웹 경험이 우선순위가 되면 별도의 브라우저 앱을 추가한다. 운영 콘솔은 소비자 앱과 권한 및 출시 주기가 다르므로 별도 웹 배포로 구성한다.

| 관심사 | 결정 |
| --- | --- |
| 모바일 런타임 | 프로젝트 초기 구성 시점의 최신 안정 Expo SDK를 사용하는 React Native |
| 언어 | `strict` 모드의 TypeScript |
| 내비게이션 | Expo Router |
| 서버 상태 | TanStack Query |
| 화면 간 로컬 상태 | URL/라우터, 서버 캐시, 폼이 소유하지 않는 상태에만 Zustand 사용 |
| 폼 | React Hook Form과 Zod 스키마 |
| 현지화 | i18next 호환 메시지 카탈로그와 `expo-localization` |
| API와 인증 | 애플리케이션 저장소 계층 뒤의 Supabase JavaScript 클라이언트 |
| 안전한 로컬 값 | `expo-secure-store`; 인증 정보에 AsyncStorage 사용 금지 |
| 오류 보고 | PII 제거를 적용한 Sentry |
| 모바일 빌드/출시 | EAS Build, Submit, Update |
| 단위/컴포넌트 테스트 | 순수 패키지는 Vitest, 모바일 UI는 Jest와 React Native Testing Library |
| E2E 테스트 | iOS/Android의 출시 유사 빌드에서 Maestro |
| 웹 애플리케이션 | `apps/web`과 `apps/admin` 구현 시 Next.js와 TypeScript |

버전은 잠금 파일과 Expo 호환성 요구사항에 따라 고정한다. 저장소에는 업그레이드 주기를 문서화하고 출시 빌드에서 유동적인 의존성 버전 범위를 사용하지 않는다.

## 2. 저장소 구조

목표 구조는 Turborepo 작업 오케스트레이션을 사용하는 pnpm workspace이다.

```text
apps/
  mobile/
    src/app/               # Expo Router 라우트 파일, 조합만 담당
    src/features/          # 프로필, 탐색, 매칭, 메시지, 안전
    src/components/        # 모바일 애플리케이션 컴포넌트
    src/lib/               # 플랫폼 어댑터와 앱 초기 설정
    tests/
  admin/                   # 별도 배포되는 최소 운영 콘솔
  web/                     # 향후 공개/회원용 웹 경험
packages/
  domain/                  # 순수 도메인 타입, 정책, Zod 스키마
  api-client/              # 저장소 인터페이스와 Supabase 구현
  design-tokens/           # 시맨틱 색상, 글자, 간격, 반경 토큰
  i18n/                    # 일본어/한국어 메시지 카탈로그와 도우미
  config/                  # 공용 린트, TypeScript, 테스트 설정
supabase/
  migrations/
  functions/
  tests/
docs/
```

화면을 억지로 여러 플랫폼에서 공유하지 않는다. 도메인 규칙, 스키마, API 계약, 현지화 키, 디자인 토큰을 공유한다. 모바일과 데스크톱의 상호작용이 다르면 각 UI 컴포넌트를 독립적으로 유지한다.

## 3. 모듈 경계

각 기능이 화면, 훅, UI, 쿼리, 뮤테이션, 테스트를 소유한다.

```text
features/messaging/
  api/
  components/
  hooks/
  screens/
  model/
  tests/
```

규칙은 다음과 같다.

- 라우트 파일은 라우트 파라미터를 해석하고 기능 화면을 조합한다. 데이터 접근이나 도메인 규칙을 포함하지 않는다.
- 컴포넌트는 Supabase 클라이언트를 직접 가져오지 않는다.
- 기능은 `packages/api-client`의 저장소 인터페이스를 호출한다.
- 기능 간 가져오기는 해당 기능의 공개 `index.ts`를 통해서만 수행한다.
- `packages/domain`은 프레임워크와 무관해야 하며 React, Expo, Supabase, 브라우저 API를 가져오지 않는다.
- 데이터베이스에서 생성한 타입은 API 어댑터의 입력이며 애플리케이션 도메인 모델이 아니다.
- 매핑 계층이 UI와 도메인 코드를 데이터베이스 컬럼 및 공급자 변경으로부터 보호한다.

## 4. 상태 소유권

가장 좁은 범위의 상태 소유자를 사용한다.

1. 컴포넌트 상호작용 상태는 로컬에 둔다.
2. 폼 값은 React Hook Form이 소유한다.
3. 라우트 식별자와 공유 가능한 필터는 Expo Router 파라미터에 둔다.
4. 원격 레코드, 로딩, 재시도, 무효화는 TanStack Query가 소유한다.
5. 위 범주에 속하지 않는 세션 인접 UI 상태만 작은 Zustand 스토어를 사용할 수 있다.

서버 레코드를 Zustand에 복제하지 않는다. 낙관적 업데이트는 되돌릴 수 있는 저위험 동작으로 제한한다. 매칭 생성, 신고 제출, 본인 인증, 구독 권한은 항상 서버 응답과 일치시킨다.

## 5. 데이터 접근과 인증

- 애플리케이션 런타임마다 설정된 Supabase 클라이언트 하나를 만든다.
- 세션은 플랫폼에 적합한 보안 저장소 어댑터로 보존한다.
- anonymous key는 공개 설정으로 취급한다. service-role key는 클라이언트에 절대 포함하지 않는다.
- Data API에 노출되는 모든 테이블에서 RLS를 활성화하고 테스트한다.
- 클라이언트의 직접 데이터 접근은 승인된 사용자 소유 CRUD와 읽기 모델로 제한한다.
- 중요한 워크플로는 저장소를 통해 버전이 지정된 Edge Function 또는 데이터베이스 함수를 호출한다.
- 쿼리 키는 기능별 팩토리로 정의하고 필요한 경우 로케일과 가시성 맥락을 포함한다.
- 본인 인증 서류는 비공개 스토리지와 수명이 짧은 승인 다운로드를 사용한다. 일반 이미지 캐시에 기록하지 않는다.

UI는 세션 갱신 실패, 접근 철회, 계정 정지, 계정 삭제를 서로 다른 결과로 처리한다.

## 6. UI 구현

- 디자인 값은 `packages/design-tokens`의 시맨틱 토큰에서만 가져온다.
- React Native 기본 요소와 Expo 모듈을 우선한다. 팀이 직접 유지해야 할 접근성 동작을 크게 줄일 때만 UI 의존성을 추가한다.
- 익숙한 기호가 있는 버튼에는 선정된 아이콘 라이브러리의 아이콘을 사용한다. 아이콘 전용 제어에는 접근 가능한 이름을 제공한다.
- 목록은 가상화하고 가능한 경우 안정적인 항목 크기를 사용한다. 프로필 미디어는 로딩 전에 종횡비 공간을 확보한다.
- 표시 크기에 맞게 이미지를 축소하고 플레이스홀더를 사용한다. 본인 인증 원본 이미지는 소비자 프로필 UI에 절대 불러오지 않는다.
- 키보드, 안전 영역, 노치, 바텀 시트, RTL 준비를 중앙에서 처리한다. 첫 지원 언어가 LTR이더라도 이 원칙을 유지한다.

## 7. 현지화

일본어(`ja`)와 한국어(`ko`)를 첫 빌드부터 함께 제공한다.

- 초기 로케일은 사용자의 명시적 선택을 우선하고 기기 설정을 대체값으로 사용한다.
- 메시지 키는 기능별 네임스페이스로 관리하고 누락 및 미사용 키를 CI에서 검증한다.
- 법적 문서는 서버에서 제공되는 버전 콘텐츠이며 클라이언트는 동의한 정확한 버전을 기록한다.
- 사용자 콘텐츠와 기계 번역을 분리 저장한다. 메시지 UI에서 언제든 원문을 볼 수 있어야 한다.
- 로케일별 스냅샷 테스트는 긴 레이블, 복수형, 날짜, 줄바꿈을 포함한다.

## 8. 오류, 오프라인, 로딩 동작

모든 쿼리 화면은 최초 로딩, 새로고침, 빈 상태, 캐시 콘텐츠가 있는 오프라인, 권한 오류, 복구 가능한 서버 오류, 최종 계정 제한을 구분한다.

- 안전하고 멱등한 읽기는 지터를 포함한 제한적 지수 백오프 재시도를 사용한다.
- API가 멱등성 키를 받지 않는 뮤테이션은 자동 재시도하지 않는다.
- 중요한 뮤테이션과 오류 보고에 클라이언트 생성 요청 ID를 첨부한다.
- 사용자 표시 오류는 안정적인 애플리케이션 오류 코드를 현지화 문구로 매핑한다. 공급자 원문 오류를 그대로 표시하지 않는다.
- 메시지는 전송 대기 상태를 표시할 수 있지만 서버 승인 후에만 전송 완료로 표시한다.
- 오프라인 프로필 편집은 명시적 초안을 보존할 수 있다. MVP에서 좋아요, 신고, 차단, 메시지는 연결이 확인되어야 한다.

## 9. 성능 예산

- 대표적인 중저가 Android 기기와 현재 지원 iPhone에서 콜드 스타트를 모니터링한다.
- 초기 라우트는 번역, 분석, 중요하지 않은 프로필 미디어를 기다리지 않는다.
- 무거운 의존성을 새로 추가할 때 풀 리퀘스트에 번들 크기 비교를 포함한다.
- 프로필 목록은 커서 페이지네이션을 사용하고 다음 한 페이지만 미리 가져온다.
- 썸네일 표시에 원본 이미지를 가져오지 않는다.
- 수동 메모이제이션을 추가하기 전에 프로필 카드와 메시지 목록의 렌더링 회귀를 측정한다.

## 10. 테스트 전략

| 계층 | 검증 범위 |
| --- | --- |
| 도메인 단위 | 매칭 자격, 가시성, 차단 규칙, 번역 상태, 유효성 검사 |
| 저장소 계약 | 매핑, 오류 정규화, 페이지네이션, 인증 만료 |
| 컴포넌트 | 접근성 역할/이름, 상태, 로케일 넘침, 사용자 상호작용 |
| 기능 통합 | 온보딩, 탐색 결정, 매칭 생성, 메시지, 신고/차단 |
| E2E | 양 플랫폼의 핵심 정상 흐름과 안전 흐름 |

테스트는 프로덕션 서비스에 의존하지 않는다. 로컬 Supabase 또는 격리된 테스트 프로젝트가 결정적인 픽스처를 제공한다. 최소 E2E 출시 게이트는 로그인, 프로필 완성, 상호 매칭, 메시지 교환, 차단, 로그아웃을 포함한다.

## 11. 배포와 환경

`development`, `preview`, `production` 설정을 분리한다. 각 설정은 별도 백엔드 프로젝트를 가리키며 필요에 따라 서로 다른 번들/애플리케이션 식별자를 사용한다.

- 풀 리퀘스트: 포맷, 린트, 타입 검사, 단위/컴포넌트 테스트, 데이터베이스 테스트, 모바일 동작 변경 시 프리뷰 빌드.
- main 브랜치: 데이터베이스 마이그레이션과 함수를 preview 환경에 배포한 뒤 스토어 베타 빌드 생성.
- production: 검토된 커밋과 마이그레이션 세트를 승격한다. 커밋되지 않은 작업 공간에서 빌드하지 않는다.
- EAS Update는 preview와 production 채널을 사용하고 앱 버전을 기준으로 `runtimeVersion`을 설정한다.
- OTA 변경은 production 전에 preview 채널에서 검증하고 스토어 정책 및 호환 가능한 네이티브 런타임 범위 안에 있어야 한다.

## 12. 초기 구성 후 표준 명령

```text
pnpm install
pnpm dev
pnpm lint
pnpm typecheck
pnpm test
pnpm test:e2e
pnpm build
```

이 명령은 목표 계약이다. workspace와 스크립트가 실제로 생성된 뒤부터 유효한 표준 명령이 된다.

## 13. 참고 자료

- [Expo Router 소개](https://docs.expo.dev/router/introduction/)
- [EAS Build](https://docs.expo.dev/build/)
- [EAS Update 배포](https://docs.expo.dev/eas-update/deployment/)
- [Expo 푸시 알림](https://docs.expo.dev/push-notifications/overview/)
