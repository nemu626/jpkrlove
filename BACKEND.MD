# jpkrlove 백엔드

## 1. 결정 요약

MVP 백엔드는 관리형 Supabase의 PostgreSQL, Auth, 비공개 Storage, Realtime, Edge Functions를 사용한다. 독립적인 마이크로서비스 집합이 아니라 **모듈형 모놀리스**로 구성한다. PostgreSQL이 단일 진실 공급원이며 모든 스키마 변경은 커밋된 SQL 마이그레이션으로 관리한다.

Supabase를 통해 인증, 관계형 데이터, 접근 제어, 파일 처리, 실시간 전달을 빠르게 구축한다. 동시에 표준 PostgreSQL, SQL 마이그레이션, 이식 가능한 TypeScript 도메인 코드로 향후 이전 가능성을 유지한다.

## 2. 책임 경계

| 컴포넌트 | 책임 |
| --- | --- |
| PostgreSQL | 진실 공급원, 제약조건, 트랜잭션, RLS, 매칭 읽기 모델, 감사 메타데이터 |
| Auth | 계정 식별과 세션 수명 주기 |
| Storage | 분리된 버킷의 프로필 미디어, 신고 증거, 본인 인증 서류 |
| Realtime | 새 메시지와 매칭 이벤트의 승인된 전달 |
| Edge Functions | 권한이 필요한 워크플로, 공급자 연동, 웹훅, 멱등성, 속도 제한 |
| Queues | 번역, 알림, 검수, 정리 작업의 내구성 있는 비동기 처리 |
| Cron | 만료, 보존, 재시도, 정합성 검사 예약 작업 |

모바일 클라이언트는 권한이 부여된 뷰를 직접 읽고 명시적으로 허용된 소유자 전용 CRUD를 수행할 수 있다. 매칭, 인증 결과, 운영 조치, 권한, 공급자 웹훅은 직접 확정할 수 없다.

## 3. 데이터베이스 구성

스키마로 노출 범위를 명시한다.

- `app`: 회원 대상 도메인 테이블과 읽기 모델.
- `private`: 인증, 운영, 공급자 참조, 내부 메타데이터. 공개 Data API에 노출하지 않는다.
- `audit`: 추가만 가능한 보안 및 운영자 이벤트.
- `public`: Supabase Data API 관례상 필요한 최소한의 안정적인 함수와 뷰.

규칙은 다음과 같다.

- 기본 키는 UUID를 사용한다. Auth 사용자 ID를 계정 식별자로 유지한다.
- 시간은 UTC `timestamptz`로 저장하고 표시 경계에서 로케일에 맞게 변환한다.
- 사용자에게 노출되는 상태의 수명 주기가 완전히 닫혀 있을 때만 PostgreSQL enum을 사용한다. 그 외에는 제약된 텍스트와 문서화된 상태 전이를 사용한다.
- 금액은 정수형 최소 통화 단위와 ISO 통화 코드로 저장한다.
- 변경 가능한 모든 레코드에 `created_at`, `updated_at`을 둔다. 민감한 상태 전환에는 수행자와 사유를 기록한다.
- 애플리케이션 유효성 검사가 있어도 외래 키, 고유 제약, check 제약으로 불변조건을 보장한다.
- 복구, 감사, 법적 의무가 있는 경우에만 소프트 삭제를 사용한다. 모든 테이블의 기본 방식으로 삼지 않는다.

## 4. 핵심 모델

| 영역 | 주요 레코드 |
| --- | --- |
| 계정 | `profiles`, `profile_preferences`, `legal_acceptances`, `account_statuses` |
| 미디어 | `profile_media`, 스토리지 객체 참조, 검수 상태 |
| 신뢰 | `verification_cases`, `verification_checks`, 공급자 참조 |
| 탐색 | `discovery_preferences`, `recommendation_events`, 제외/읽기 뷰 |
| 매칭 | `likes`, `matches`, `unmatches` |
| 메시지 | `conversations`, `conversation_members`, `messages`, `message_translations` |
| 안전 | `blocks`, `reports`, `report_evidence`, `moderation_actions` |
| 전달 | `device_installations`, `notification_preferences`, `notification_deliveries` |
| 수익화 | 구독 활성화 시 `entitlements`, `purchase_events` |
| 운영 | `operator_roles`, `case_assignments`, `audit_events` |

프로필 필드는 자격이 있는 회원에게 공개, 매칭 전용, 운영 전용, 수집 금지로 분류한다. 민감한 필드를 추가하는 마이그레이션에 해당 분류를 문서화한다.

## 5. 권한 부여

모든 Data API 테이블과 스토리지 객체 테이블에서 RLS를 의무화한다. 정책 없이 새 테이블을 노출하면 CI가 실패해야 한다.

### 5.1 회원 규칙

- 회원은 자신의 프로필에서 허용된 필드만 수정할 수 있다.
- 양쪽 계정이 자격 조건을 충족하고 서로 차단하지 않았으며 탐색 공개 정책이 허용할 때만 다른 프로필을 볼 수 있다.
- 좋아요는 제품 권한에 따라서만 공개하며 차단 또는 제한된 계정을 노출하지 않는다.
- 메시지를 읽으려면 대화 참여자여야 한다.
- 새 메시지는 활성 매칭과 동일한 서버 워크플로 안에서 최종 차단 상태를 확인해야 한다.
- 신고자는 신고 접수 및 상태 요약만 확인할 수 있다. 증거와 내부 메모는 운영 전용이다.
- 본인 인증 서류와 공급자 원본 응답은 Data API를 통해 회원이 읽을 수 없어야 한다.

### 5.2 운영자 규칙

운영자는 MFA와 최소 권한 역할을 갖춘 별도 웹 앱을 사용한다. 역할은 고객 지원, 인증 검토, 운영 검토, 관리자로 나눈다. service-role 자격 증명은 서버 코드에서만 사용한다. 브라우저 코드는 service-role Supabase 클라이언트가 아니라 범위가 제한된 서버 응답을 받는다.

본인 인증 서류, 신고, 계정 제한, 계정 내보내기를 조회하거나 변경할 때마다 수행자, 동작, 대상, 시간, 요청 ID, 필요한 경우 사유를 포함한 감사 이벤트를 생성한다.

## 6. 스토리지와 미디어

정책이 서로 다른 별도 비공개 버킷을 사용한다.

| 버킷 | 접근 및 보존 |
| --- | --- |
| `profile-media` | 정책 또는 단기 URL을 통한 자격 있는 인증 사용자의 접근. 검수된 파생 파일만 사용 |
| `identity-documents` | 제출자의 업로드와 승인된 인증 워크플로만 접근. 공개 금지 |
| `report-evidence` | 신고자 업로드와 승인된 운영 검토 접근 |

- MIME 타입, 매직 바이트, 파일 크기, 이미지 크기, 업로드 소유권을 검증한다.
- 회원에게 공개되는 파생 파일에서 EXIF와 위치 메타데이터를 제거한다.
- 업로드를 비동기로 처리하고 검증이 성공할 때까지 격리한다.
- 법률이나 공급자 정책이 다른 기간을 요구하지 않는 한 인증 및 이의 제기 기간이 끝난 뒤 본인 인증 서류를 삭제한다.
- 원본 서류를 무기한 보관하지 않고 최소 인증 결과와 공급자 참조만 저장한다.
- 서명된 URL은 수명을 짧게 설정하고 전체 값을 로그에 기록하지 않는다.

## 7. 중요 워크플로

### 7.1 좋아요와 상호 매칭

하나의 트랜잭션형 서버 작업으로 처리한다.

1. 수행자를 인증하고 속도 제한을 적용한다.
2. 양쪽 계정 상태, 연령/자격, 기존 차단, 중복 결정을 확인한다.
3. 좋아요를 멱등하게 삽입한다.
4. 반대 방향 좋아요가 있으면 하나의 고유 매칭과 대화를 생성한다.
5. 커밋 후 일반적인 알림을 큐에 넣는다.
6. 기준이 되는 매칭 결과를 반환한다.

순서와 무관한 고유 사용자 쌍 키로 동시 실행 시 중복 매칭을 막는다.

### 7.2 메시지 전송

1. 대화 참여, 활성 매칭, 현재 차단 상태, 콘텐츠 길이, 운영 제한을 검증한다.
2. 클라이언트 멱등성 키와 함께 원문 메시지를 삽입한다.
3. 번역과 알림 작업을 큐에 넣는다.
4. Realtime은 승인된 대화 참여자에게만 이벤트를 전달한다.
5. 번역은 원문/대상 언어, 공급자 버전, 상태, 시간을 포함한 별도 파생 레코드에 기록한다.

### 7.3 본인 인증

1. 인증 케이스와 수명이 짧은 승인 업로드를 생성한다.
2. `VerificationProvider` 어댑터를 통해 서류 또는 공급자 세션을 전송한다.
3. 서명된 공급자 웹훅을 검증하고 원본 응답은 최소 진단 기간만 보관한다.
4. 검토된 상태 전이를 적용하며 모호한 결과는 운영자 큐로 보낸다.
5. 보존 정책에 따라 원본 서류를 직접 삭제하거나 공급자에게 삭제를 위임한다.

인증 공급자는 교체할 수 있어야 한다. 각각 별도 검증되지 않는 한 제품 문구에서 본인 인증이 독신 상태, 소득, 인격까지 증명한다고 표현하지 않는다.

### 7.4 신고와 차단

차단은 동기적으로 처리하며 양쪽 방향의 탐색과 새 메시지를 즉시 막는다. 신고는 차단 후에도 사용할 수 있는 별도 케이스이다. 심각도가 높은 분류는 수동 검토 전까지 공개를 제한할 수 있지만 영구 제재는 문서화된 운영 정책을 따라야 한다.

## 8. 비동기 처리

내구성 있는 작업에 Supabase Queues/`pgmq`를 사용한다. 큐는 서버 전용이며 회원 클라이언트에 노출하지 않는다.

초기 큐는 다음과 같다.

- `translate-message`
- `send-notification`
- `moderate-media`
- `retention-cleanup`
- `provider-reconciliation`

각 작업은 스키마 버전, 요청/상관관계 ID, 멱등성 키, 시도 횟수, 가능한 경우 민감한 페이로드 대신 리소스 참조를 포함한다. 소비자는 성공한 뒤에만 작업을 삭제하거나 보관한다. 실패에는 제한적 재시도와 운영자가 확인할 수 있는 dead-letter 정책을 적용한다.

Edge Functions는 짧게 실행되는 오케스트레이터로 사용한다. 무거운 미디어 처리나 지속적인 큐 소비가 실행 제한 또는 큐 지연에 영향을 주면 전용 워커로 이동한다.

## 9. API 계약

- Edge Function 경계의 외부 애플리케이션 작업은 `/v1` 아래에서 버전 관리한다.
- 요청과 응답 본문은 공유 Zod 스키마 및 생성 또는 정적 TypeScript 타입을 사용한다.
- 탐색, 좋아요, 매칭, 메시지, 감사 목록에는 커서 페이지네이션을 의무화한다.
- 중요한 POST 요청은 `Idempotency-Key`를 받고 재전송 시 기존 결과를 반환한다.
- 응답에 `requestId`를 포함하고 로그도 같은 값을 사용한다.
- 오류는 `ACCOUNT_RESTRICTED`, `MATCH_NOT_ACTIVE`, `RATE_LIMITED`, `VERIFICATION_REQUIRED` 같은 안정적인 코드를 사용한다.
- 원본 SQL, 공급자 오류, 스토리지 경로, 내부 정책 사유를 클라이언트에 반환하지 않는다.

## 10. 보안과 개인정보 기준

- 전송 중 TLS와 공급자 관리 저장 암호화를 의무화한다.
- 비밀 값은 환경/비밀 저장소에 두고 주기적으로 교체한다. 커밋하거나 OTA 번들에 포함하지 않는다.
- 프로덕션과 비프로덕션은 프로젝트, 키, 스토리지, 데이터를 분리한다.
- 시드 데이터는 합성 데이터만 사용한다. 프로덕션 개인정보를 개발 환경으로 복사하지 않는다.
- 로그와 오류 보고에서 토큰, 메시지 본문, 서류 URL, 정확한 생년월일, 공급자 응답을 제거한다.
- 인증, 본인 인증, 메시지, 좋아요, 신고, 파일 업로드에 각각 독립적인 속도 제한을 적용한다.
- 계정 탐색으로 이메일이나 전화번호의 가입 여부가 드러나지 않게 한다.
- 데이터 내보내기와 삭제 워크플로는 감사 가능하고 재시도할 수 있어야 하며 외부 공급자와 정합성을 맞춰야 한다.
- 개인정보 영향 평가와 일본/한국 법률 검토를 출시 후 작업이 아니라 출시 게이트로 둔다.

## 11. 관측성과 운영

- 구조화된 JSON 로그에 환경, 함수, 요청 ID, 적절한 경우 수행자 ID, 처리 시간, 결과 코드를 포함한다.
- Sentry는 `sendDefaultPii`를 비활성화하고 명시적으로 민감 정보를 제거한 예외와 트레이스를 받는다.
- API 지연/오류율, 인증 실패, 큐 대기 시간/깊이, 번역 실패, 푸시 영수증, 인증 적체, 신고 SLA, 스토리지 처리 실패를 측정한다.
- 알림은 실제 조치가 가능해야 하며 담당자와 실행 절차로 연결한다.
- 프로덕션에서 백업과 시점 복구를 활성화한다. 출시 전과 최소 분기마다 복원 훈련을 한다.

## 12. 데이터베이스와 백엔드 테스트

- pgTAP으로 허용 및 거부되는 신원을 사용해 모든 RLS 정책을 테스트한다.
- 마이그레이션 테스트는 빈 데이터베이스 구축과 프로덕션 유사 이전 스키마의 업그레이드를 모두 검증한다.
- 함수 테스트는 인증, 유효성 검사, 멱등성, 동시성, 공급자 서명 검증, 오류 정규화를 포함한다.
- 공급자 어댑터마다 픽스처와 가능한 경우 샌드박스 엔드포인트를 사용하는 계약 테스트를 실행한다.
- 탐색 페이지네이션, 매칭 동시성, 메시지 팬아웃, 큐 적체 복구를 부하 테스트한다.

대시보드에서만 수행하는 스키마 또는 정책 변경은 허용하지 않는다. 긴급 변경도 즉시 검토 가능한 마이그레이션으로 내보내야 한다.

## 13. 추가 결정이 필요한 공급자

플랫폼은 결정되었지만 다음 연동은 선정 전에 법률, 지원 범위, 품질, 비용 평가가 필요하다.

- 일본과 한국을 지원하는 본인 인증 공급자.
- 번역 공급자와 허용 가능한 데이터 처리 조건.
- 트랜잭션 이메일/SMS 공급자.
- 구독 공급자와 App Store/Play 정책 구현.
- 법인, 데이터 거주 요건, 일본/한국 지연 측정에 따른 프로덕션 데이터베이스 리전.

각 연동은 작은 포트(`VerificationProvider`, `TranslationProvider`, `NotificationProvider`, `EntitlementProvider`) 뒤에 두고 선정 시 ADR로 기록한다.

## 14. 참고 자료

- [Supabase 데이터베이스와 RLS](https://supabase.com/docs/guides/database/overview)
- [Supabase 비공개 스토리지 버킷](https://supabase.com/docs/guides/storage/buckets/fundamentals)
- [Supabase Edge Functions](https://supabase.com/docs/guides/functions)
- [Supabase Queues](https://supabase.com/docs/guides/queues)
- [Supabase에서 pgTAP 사용](https://supabase.com/docs/guides/database/extensions/pgtap)
