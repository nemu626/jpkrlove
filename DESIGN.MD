# jpkrlove 제품 디자인

## 1. 목적과 문서 상태

이 문서는 첫 iOS/Android MVP의 제품 디자인 기준을 정의한다. `docs/product-planning.md`를 기반으로 하며, 현실적으로 결혼을 고려하는 진지한 관계를 원하는 일본인과 한국인을 연결하는 제품을 대상으로 한다.

디자인은 신뢰, 신중한 선택, 이중 언어 소통, 사용자 안전을 우선해야 한다. 체류 시간 극대화를 목적으로 하는 대량 스와이프형 앱을 모방하지 않는다.

## 2. 디자인 원칙

1. **탐색보다 신뢰 우선**: 성장 장치보다 인증 상태, 관계 의향, 안전 제어를 먼저 보여 준다.
2. **진지하되 경직되지 않게**: 차분하고 프리미엄한 인상을 유지하되, 쉬운 문장과 따뜻한 사진으로 신청서 같은 느낌을 피한다.
3. **두 언어를 동등하게**: 일본어와 한국어를 모두 1급 언어로 취급한다. 어느 한쪽도 부차적인 번역 계층으로 표현하지 않는다.
4. **중요한 행동의 결과 설명**: 좋아요, 매칭, 신고, 차단, 인증, 결제는 확정 전에 결과를 명확히 안내한다.
5. **점진적 공개**: 민감하거나 상세한 프로필 질문은 목적을 설명하며 단계별로 수집한다.
6. **모호하지 않은 안전 기능**: 모든 프로필과 대화에서 신고, 차단, 도움말에 접근할 수 있어야 한다.

## 3. MVP 사용자 경험 범위

### 3.1 포함

- 계정 생성과 로그인.
- 연령 확인, 이용약관, 개인정보 처리 동의, 커뮤니티 가이드라인 동의.
- 일본어/한국어 선택.
- 프로필 생성: 거주지, 국적, 관계 및 결혼 의향, 언어 수준, 희망 거주 국가, 자기소개, 사진.
- 본인 인증 제출과 상태 확인.
- 프로필 추천과 필터링.
- 좋아요, 상호 매칭, 매칭 목록, 일대일 메시지.
- 원문과 번역문 표시. 원문은 항상 확인할 수 있어야 한다.
- 푸시 알림 설정.
- 신고, 차단, 매칭 해제, 계정 일시 정지, 데이터 내보내기 요청, 계정 삭제 요청.
- 본인 인증, 신고, 계정 제한을 처리하는 최소 운영 콘솔.

### 3.2 명시적으로 제외

- 공개 소셜 피드, 스토리, 라이브 스트리밍, 선물, 연속 접속 보상.
- 음성 및 영상 통화.
- 정밀 실시간 위치.
- 전문가 조언처럼 제시되는 AI 연애 상담.
- 설명할 수 없는 자동 궁합 점수.
- 운영 모델이 검증되기 전의 상담사 예약과 프리미엄 컨시어지 흐름.

## 4. 정보 구조

로그인한 모바일 앱은 아이콘과 텍스트 레이블이 있는 네 개의 하단 메뉴를 사용한다.

| 메뉴 | 목적 |
| --- | --- |
| 둘러보기 | 선별된 추천과 명시적인 필터 |
| 받은 좋아요 | 요금제 권한에 따른 받은 좋아요와 미결정 항목 |
| 메시지 | 매칭과 대화 |
| 프로필 | 내 프로필, 인증, 선호 조건, 안전, 설정 |

온보딩과 인증은 탭 셸 바깥의 스택 흐름으로 구성한다. 신고, 차단, 번역 상세, 필터, 확인은 내용 길이에 따라 시트 또는 전체 화면을 사용한다. 파괴적인 동작의 확인을 일시적인 토스트에만 맡기지 않는다.

## 5. 핵심 사용자 흐름

### 5.1 온보딩

1. 인터페이스 언어를 선택한다. 이후에도 변경할 수 있다.
2. 이용 가능 연령을 확인하고 버전이 명시된 법적 문서에 동의한다.
3. 초기 지원 인증 수단으로 계정을 생성한다.
4. 기본 프로필과 관계 의향을 입력한다.
5. 사용할 수 있는 프로필 사진을 한 장 이상 추가한다.
6. 다른 사용자에게 보이는 그대로 프로필을 검토한다.
7. 본인 인증을 제출하거나 제한 사항이 명확한 상태로 계속한다.

각 섹션이 끝날 때마다 진행 상황을 저장한다. 민감한 필드를 요청하는 이유, 공개 여부, 매칭에만 쓰이는지 여부를 사용자에게 보여 준다.

### 5.2 탐색과 매칭

- 추천은 제스처에만 의존하는 스와이프 덱이 아니라 세로 스크롤 프로필로 제공한다.
- 주요 선택 동작은 `좋아요`와 `다음`이다. 실행 취소는 제품 정책이 허용할 때만 제공한다.
- 필수 조건과 선호 조건을 분리한다.
- 상호 좋아요는 원자적으로 매칭을 생성하고 짧은 이중 언어 대화 시작 문구를 제공한다.
- 빈 상태에서는 사용자를 압박하지 않고 필터 완화처럼 다음으로 유용한 행동을 안내한다.

### 5.3 메시지와 번역

- 모든 번역 메시지에 번역문임을 표시하고 `원문 보기`를 제공한다.
- 번역이 실패해도 원문은 숨기지 않는다.
- 기계 번역으로 사용자 의도를 알리지 않고 바꾸지 않는다.
- 푸시 알림은 일반적인 문구를 사용하며 기본적으로 메시지 본문을 포함하지 않는다.
- 매칭 해제 후에도 대화 헤더에서 차단과 신고에 접근할 수 있어야 한다.

### 5.4 안전과 본인 인증

- 인증 상태는 색상만이 아니라 텍스트와 아이콘을 함께 사용한다.
- `제출됨`, `검토 중`, `인증 완료`, `조치 필요`, `만료됨`을 서로 다른 상태로 취급한다.
- 본인 확인 서류 이미지는 다른 회원에게 절대 노출하지 않는다.
- 차단은 UI에서 즉시 적용되며 양쪽 사용자를 탐색과 메시지에서 제외한다.
- 신고는 분류, 선택적 상세 내용, 증거 첨부를 지원하고 접수 번호를 제공한다.

## 6. 비주얼 시스템

방향성은 **차분하고 신뢰할 수 있으며 현대적인 디자인**이다. 프리미엄 소개 서비스와 친근한 커뮤니케이션 제품 사이를 지향한다. 국가에 대한 고정관념, 과도한 국기 장식, 성별을 구분하는 색상, 기본적인 결혼식 이미지는 피한다.

### 6.1 색상 역할

접근 가능한 정확한 값은 비주얼 디자인 단계에서 확정하되, 토큰 역할은 고정한다.

| 토큰 역할 | 방향 | 용도 |
| --- | --- | --- |
| `surface` | 흰색 또는 흰색에 가까운 중립색 | 기본 캔버스 |
| `text.primary` | 차콜 | 주요 텍스트 |
| `text.secondary` | 중간 명도의 중립색 | 보조 텍스트 |
| `brand.primary` | 절제된 코럴 | 주요 행동과 선택 상태 |
| `brand.secondary` | 짙은 틸 | 신뢰와 보조 강조 |
| `status.success` | 초록색 | 인증과 성공 |
| `status.warning` | 앰버 | 주의 필요 |
| `status.danger` | 빨간색 | 파괴적 동작과 안전 조치 |

장식용 그라데이션은 사용하지 않는다. 텍스트와 필수 제어 요소는 WCAG 2.2 AA 대비를 충족한다. 첫 MVP에서 다크 모드는 필수가 아니지만, 기능 코드의 시맨틱 토큰이 밝은 값만 전제로 해서는 안 된다.

### 6.2 타이포그래피와 간격

- MVP에서는 자연스러운 렌더링과 작은 바이너리 크기를 위해 일본어와 한국어에 플랫폼 시스템 글꼴을 사용한다.
- 글자 크기 단계는 논리 픽셀 기준 12, 14, 16, 20, 24, 32이다. 운영 화면 내부에서는 히어로 크기 텍스트를 사용하지 않는다.
- 플랫폼 제어 요소의 기본 동작을 제외하고 자간은 `0`으로 한다.
- 8포인트 간격 그리드를 사용하고 아이콘과 레이블 정렬에만 4포인트 예외를 허용한다.
- 플랫폼 관례에 따라 제어 높이는 iOS에서 최소 44포인트, Android에서 최소 48dp로 한다.
- 모서리 반경은 4 또는 8을 사용한다. 완전한 필 형태는 태그, 상태, 세그먼트 제어에만 사용한다.

### 6.3 컴포넌트

초기 디자인 시스템은 Button, IconButton, TextField, TextArea, Select, Checkbox, RadioGroup, Switch, SegmentedControl, Slider, Avatar, VerificationBadge, ProfilePhoto, FilterChip, Banner, EmptyState, Dialog, Sheet, Snackbar, Skeleton, InlineError를 포함한다. IconButton에는 툴팁 또는 접근성 레이블이 있어야 한다.

모든 컴포넌트는 로딩, 비활성화, 포커스, 눌림, 오류, 오프라인 상태를 정의해야 완성된 것으로 본다.

## 7. 콘텐츠와 현지화

- 원문 카피는 컴포넌트에 직접 넣지 않고 안정적인 메시지 키로 관리한다.
- 일본어와 한국어 카피는 출시 전에 사람이 검토한다. 법률, 안전, 결제, 본인 인증 문구에 기계 번역을 사용하지 않는다.
- 날짜, 이름, 주소, 숫자 형식, 줄바꿈은 선택한 로케일을 따른다.
- 번역된 문장 조각을 이어 붙이지 않는다. 이름 있는 변수를 포함한 완전한 ICU 형식 메시지를 사용한다.
- 사용자 생성 콘텐츠는 알 수 있는 경우 원문 언어를 저장한다. 번역은 별도의 파생 레코드로 관리한다.
- 어조는 존중을 담되 직접적이어야 한다. 은어, 성별 가정, 결혼 성공을 보장하는 표현은 피한다.

## 8. 접근성과 개인정보 UX

- 스크린 리더 레이블은 동작과 상태를 모두 설명한다.
- 포커스 순서는 시각적 순서를 따른다. 시트와 다이얼로그는 포커스를 가두고 닫을 때 이전 위치로 복원한다.
- 플랫폼의 큰 접근성 글자 크기에서도 사용할 수 있어야 한다. 텍스트를 자르는 대신 레이아웃을 재배치할 수 있다.
- 모션 감소 설정을 존중한다. 어떤 결정도 애니메이션에 의존하지 않는다.
- 사진 설명과 프로필 구조는 비공개 메타데이터를 노출하지 않으면서 필요한 맥락을 제공한다.
- 기술적으로 지원되는 경우 앱 전환 화면에서 민감한 화면을 숨긴다. Android에서는 본인 인증 서류 화면의 캡처를 막고 플랫폼 제약을 문서화한다.
- 비공개 콘텐츠의 클립보드 사용은 명시적이어야 하며, 지원되는 경우 시간제한을 적용한다.

## 9. 디자인 검증

MVP 출시 전에 대표 사용자와 함께 일본어와 한국어로 다음 작업을 검증한다.

- 프로필을 완성하고 어떤 필드가 공개되는지 이해한다.
- 본인 인증이 무엇을 보장하고 무엇을 보장하지 않는지 설명한다.
- 적합한 프로필을 찾고 좋아요를 보낸다.
- 작성 주체를 혼동하지 않고 원문과 번역문을 읽는다.
- 도움 없이 유해 사용자를 신고하고 차단한다.
- 계정을 일시 정지하거나 삭제하고 결과를 이해한다.

필수 디자인 QA는 작은 화면과 큰 화면의 iPhone, 소형 Android 화면, 동적 글자 크기, 스크린 리더, 키보드 표시, 빈 상태/오류/오프라인 상태, 긴 일본어와 한국어 문자열을 포함한다.

## 10. 미결정 제품 디자인 항목

- 프리미엄 소개 서비스형 어조와 친근한 대화형 어조 중 우선 방향.
- 초기 로그인 수단과 휴대전화 인증 의무화 여부.
- 탐색, 좋아요, 메시지 중 어느 단계 전에 본인 인증을 요구할지.
- 첫 출시 지역과 연령대 우선순위.
- 받은 좋아요와 고급 필터의 노출 및 가격 정책.

이 결정에는 사용자 조사, 운영 정책, 법률 검토가 필요하다. 구현의 기본 동작을 바꾸기 전에 제품 의사결정 기록으로 남겨야 한다.
